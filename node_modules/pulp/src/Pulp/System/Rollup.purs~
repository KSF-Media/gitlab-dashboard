module Pulp.System.Rollup where

import Prelude
import Control.Promise (Promise)
import Control.Promise as Promise

import Pulp.System.FFI (EffN)

type RollupOptions =
  { entry :: String
  }

type BundleOptions =
  { dest 

foreign import data Bundle :: Type

foreign import rollup :: Options -> EffN (Promise Bundle)

foreign import generate :: Bundle -> { code :: String, map :: String }

foreign import write :: Bundle -> EffN (Promise Unit)
